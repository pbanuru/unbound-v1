[project]
name = "deep-researcher"
version = "0.1.0"
description = "CLI tool for OpenAI deep research models"
requires-python = ">=3.8"
dependencies = [
    "openai>=1.59.0",
    "rich>=13.0.0",
]

[project.scripts]
deep-research = "deep_research:main"
